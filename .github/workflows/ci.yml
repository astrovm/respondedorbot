name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    # Main is protected by the PR checks; avoid running this validation again after merge.
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-26.04
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Rust toolchain
        run: rustup show

      - name: Cache Rust build
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2

      - name: Check Rust formatting
        run: cargo fmt --all -- --check

      - name: Lint Rust workspace
        run: cargo clippy --locked --workspace --all-targets --all-features -- -D warnings

  coverage:
    # Main is protected by the PR checks; avoid running this validation again after merge.
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-26.04
    permissions:
      contents: read
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_DB: respondedorbot_test
          POSTGRES_PASSWORD: synthetic-password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d respondedorbot_test"
          --health-interval 2s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis/redis-stack-server:7.4.0-v8
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 2s
          --health-timeout 5s
          --health-retries 20
    env:
      TEST_DATABASE_URL: postgresql://postgres:synthetic-password@127.0.0.1:5432/respondedorbot_test?sslmode=disable
      TEST_REDIS_HOST: 127.0.0.1
      TEST_REDIS_PORT: 6379

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Rust toolchain
        run: rustup show

      - name: Cache Rust build
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@4cef1412cce204788f482e778a0b9187f9626a29 # v2.87.21
        with:
          tool: cargo-llvm-cov@0.9.1

      - name: Install media test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends ffmpeg

      - name: Check Rust coverage
        run: |
          rustup component add llvm-tools-preview
          cargo llvm-cov clean --workspace
          # Serial: the billing schema test reruns migrations that rescale every credit row.
          cargo llvm-cov --locked --workspace --all-features --no-report -- --test-threads=1
          cargo llvm-cov report -p bot-core --fail-under-lines 95
          cargo llvm-cov report -p bot-adapters --fail-under-lines 95
          cargo llvm-cov report -p botd --fail-under-lines 95

  image:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-26.04
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4.4.1

      - name: Set image creation timestamp
        id: image-metadata
        run: echo "created=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"

      - name: Build image
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: .
          file: Containerfile
          load: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ steps.image-metadata.outputs.created }}
          cache-from: type=gha,scope=respondedorbot
          cache-to: type=gha,mode=max,scope=respondedorbot

      - name: Verify image
        run: |
          mkdir -p "$RUNNER_TEMP/bot-workspace"
          printf '%s\n' 'synthetic personality' > "$RUNNER_TEMP/bot-workspace/SOUL.md"
          printf '%s\n' 'synthetic response rules' > "$RUNNER_TEMP/bot-workspace/RULES.md"
          docker run --rm \
            --env TELEGRAM_TOKEN=synthetic-telegram-token \
            --env TELEGRAM_USERNAME=synthetic_test_bot \
            --env SUPABASE_POSTGRES_URL=postgresql://synthetic-user:synthetic-password@db.synthetic.invalid:5432/postgres?sslmode=require \
            --env COINMARKETCAP_KEY=synthetic-coinmarketcap-key \
            --env OPENROUTER_API_KEY=synthetic-openrouter-key \
            --volume "$RUNNER_TEMP/bot-workspace:/app/workspace:ro" \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}" \
            /usr/local/bin/botd --check-config

  publish-latest:
    if: github.event_name == 'push'
    runs-on: ubuntu-26.04
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4.4.1

      - name: Set image creation timestamp
        id: image-metadata
        run: echo "created=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"

      - name: Build image
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: .
          file: Containerfile
          load: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ steps.image-metadata.outputs.created }}
          cache-from: type=gha,scope=respondedorbot
          cache-to: type=gha,mode=max,scope=respondedorbot

      - name: Verify image
        run: |
          mkdir -p "$RUNNER_TEMP/bot-workspace"
          printf '%s\n' 'synthetic personality' > "$RUNNER_TEMP/bot-workspace/SOUL.md"
          printf '%s\n' 'synthetic response rules' > "$RUNNER_TEMP/bot-workspace/RULES.md"
          docker run --rm \
            --env TELEGRAM_TOKEN=synthetic-telegram-token \
            --env TELEGRAM_USERNAME=synthetic_test_bot \
            --env SUPABASE_POSTGRES_URL=postgresql://synthetic-user:synthetic-password@db.synthetic.invalid:5432/postgres?sslmode=require \
            --env COINMARKETCAP_KEY=synthetic-coinmarketcap-key \
            --env OPENROUTER_API_KEY=synthetic-openrouter-key \
            --volume "$RUNNER_TEMP/bot-workspace:/app/workspace:ro" \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}" \
            /usr/local/bin/botd --check-config

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish verified image
        run: |
          REPO="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          docker push "${REPO}:sha-${{ github.sha }}"
          docker push "${REPO}:latest"
