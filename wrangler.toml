name = "respondedorbot"
main = "build/worker/shim.mjs"
compatibility_date = "2024-11-05"
workers_dev = true

[build]
command = "cargo install -q worker-build && worker-build --release --features worker --no-default-features"
