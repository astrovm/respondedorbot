{
  "grouping_key": "operation_id",
  "hold_events": ["ai_reserve", "ai_refund"],
  "required_fields": ["user_id", "metadata.operation_id"],
  "settlement_events": ["ai_settlement_result", "memory_compaction_settlement"],
  "provider_segment_order": "ledger id ascending",
  "operation_order": "first hold created_at ascending",
  "limit": {"minimum": 1, "maximum": 500, "default": 100},
  "timestamp_type": "timezone-aware instant",
  "failure_behavior": "typed read error; reconciliation performs no mutation"
}
