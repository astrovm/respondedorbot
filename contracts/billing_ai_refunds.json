{
  "default_event": "ai_refund",
  "lock_order": ["user", "chat"],
  "source_fallback": "user",
  "optional_idempotency_key": "metadata.idempotency_key",
  "settlement_guard": ["user_id", "operation_id"],
  "duplicate_behavior": "return current locked balances without mutation",
  "transaction_attempts": 3,
  "uncertain_failure": "fail closed"
}
